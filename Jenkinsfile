pipeline {
  agent any
  stages {
    stage('') {
      steps {
        writeFile(file: 'testfile', text: 'Text File Create use pipeline')
        sh 'echo pwd'
        echo 'first stage'
      }
    }

  }
}